---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: hosts.cloudkit.openshift.io
spec:
  group: cloudkit.openshift.io
  names:
    kind: Host
    listKind: HostList
    plural: hosts
    shortNames:
    - h
    singular: host
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.powerState
      name: Power State
      type: string
    - jsonPath: .status.hostReference.hostPool
      name: Host Pool
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Host is the Schema for the hosts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Host
            properties:
              powerState:
                description: PowerState defines the desired power state of the host
                enum:
                - HOST_POWER_STATE_UNSPECIFIED
                - HOST_POWER_STATE_ON
                - HOST_POWER_STATE_OFF
                type: string
            type: object
          status:
            description: status defines the observed state of Host
            properties:
              conditions:
                description: Conditions holds an array of metav1.Condition that describe
                  the state of the Host
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              hostReference:
                description: Reference to the resources created for this Host
                properties:
                  hostPool:
                    description: HostPool that this host is assigned to, if any
                    type: string
                  namespace:
                    description: Namespace that contains the Host resources
                    type: string
                required:
                - namespace
                type: object
              phase:
                description: Phase provides a single-value overview of the state of
                  the Host
                enum:
                - Progressing
                - Failed
                - Ready
                - Deleting
                type: string
              powerState:
                description: PowerState reflects the current power state of the host
                enum:
                - HOST_POWER_STATE_UNSPECIFIED
                - HOST_POWER_STATE_ON
                - HOST_POWER_STATE_OFF
                type: string
              state:
                description: State indicates the overall state of the host from the
                  fulfillment service
                enum:
                - Unspecified
                - Progressing
                - Ready
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
